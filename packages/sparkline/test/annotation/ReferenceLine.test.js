// import React from 'react';
// import { shallow } from 'enzyme';
import { ReferenceLine } from '../src/';

describe('<ReferenceLine />', () => {
  test('it should be defined', () => {
    expect(ReferenceLine).toBeDefined();
  });
});
