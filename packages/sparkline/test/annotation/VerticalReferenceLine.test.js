// import React from 'react';
// import { shallow } from 'enzyme';
import { VerticalReferenceLine } from '../../src/';

describe('<VerticalReferenceLine />', () => {
  test('it should be defined', () => {
    expect(VerticalReferenceLine).toBeDefined();
  });
});
